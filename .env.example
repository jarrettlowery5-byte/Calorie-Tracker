# Copy this file to .env and paste your Anthropic API key after the equals sign.
# Get a key at https://console.anthropic.com/settings/keys
# The key is read ONLY by the backend (server/) and is never sent to the browser.
ANTHROPIC_API_KEY=

# Optional: port for the Express backend (defaults to 3001)
# PORT=3001
